<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>KhataBook - Track Your Transactions</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50">
    <nav class="bg-white shadow-md">
      <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center">
            <a href="/" class="text-2xl font-bold text-blue-600">
              <i class="fas fa-book-open mr-2"></i>KhataBook
            </a>
          </div>
          <div class="flex items-center gap-4">
            <div class="hidden md:flex items-center gap-2">
              <button class="p-2 rounded-full bg-gray-200 hover:bg-gray-300">
                <i class="fas fa-moon text-gray-600"></i>
              </button>
              <button class="p-2 rounded-full bg-gray-200 hover:bg-gray-300">
                <i class="fas fa-sun text-gray-600"></i>
              </button>
            </div>
            <div class="flex items-center gap-3">
              <% if (isLoggedIn) { %>
                <a href="/Dashboard" class="px-4 py-2 rounded-md bg-blue-600 text-white hover:bg-blue-700">Dashboard</a>
                <a href="/logout" class="px-4 py-2 rounded-md border border-blue-600 text-blue-600 hover:bg-blue-50">Logout</a>
              <% } else { %>
                <a href="/login" class="px-4 py-2 rounded-md bg-blue-600 text-white hover:bg-blue-700">Login</a>
                <a href="/register" class="px-4 py-2 rounded-md border border-blue-600 text-blue-600 hover:bg-blue-50">Register</a>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 py-8">
      <div class="text-center mb-12">
        <h1 class="text-4xl font-bold text-gray-900 mb-4">Welcome to KhataBook</h1>
        <p class="text-xl text-gray-600">Track your transactions and manage your accounts efficiently</p>
      </div>

      <div class="grid md:grid-cols-2 gap-8 mb-12">
        <div class="bg-white p-6 rounded-lg shadow-md">
          <div class="text-blue-600 text-4xl mb-4">
            <i class="fas fa-lock"></i>
          </div>
          <h2 class="text-2xl font-semibold mb-3">Secure Transactions</h2>
          <p class="text-gray-600">Keep your financial records safe with our encrypted storage system.</p>
        </div>

        <div class="bg-white p-6 rounded-lg shadow-md">
          <div class="text-blue-600 text-4xl mb-4">
            <i class="fas fa-chart-line"></i>
          </div>
          <h2 class="text-2xl font-semibold mb-3">Track Progress</h2>
          <p class="text-gray-600">Monitor your financial growth with detailed analytics and reports.</p>
        </div>
      </div>

      <% if (!isLoggedIn) { %>
        <div class="bg-blue-50 rounded-lg p-8 text-center">
          <h2 class="text-2xl font-semibold mb-4">Ready to Get Started?</h2>
          <p class="text-gray-600 mb-6">Join thousands of users who trust KhataBook for their transaction management</p>
          <div class="flex justify-center gap-4">
            <a href="/register" class="px-6 py-3 bg-blue-600 text-white rounded-md hover:bg-blue-700">Create Account</a>
            <a href="/login" class="px-6 py-3 border border-blue-600 text-blue-600 rounded-md hover:bg-blue-50">Sign In</a>
          </div>
        </div>
      <% } else { %>
        <div class="bg-blue-50 rounded-lg p-8 text-center">
          <h2 class="text-2xl font-semibold mb-4">Welcome Back!</h2>
          <p class="text-gray-600 mb-6">Continue managing your transactions and track your financial progress</p>
          <div class="flex justify-center gap-4">
            <a href="/Dashboard" class="px-6 py-3 bg-blue-600 text-white rounded-md hover:bg-blue-700">Go to Dashboard</a>
            <a href="/createHisab" class="px-6 py-3 border border-blue-600 text-blue-600 rounded-md hover:bg-blue-50">create Hisab</a>
          </div>
        </div>
      <% } %>
    </main>

    <footer class="bg-gray-800 text-white mt-12 py-8">
      <div class="max-w-7xl mx-auto px-4">
        <div class="text-center">
          <p>&copy; 2023 KhataBook. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
