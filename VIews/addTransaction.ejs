<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Transaction - KhataBook</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50">
    <%- include('partials/header', { 
      title: 'Add Transaction',
      isLoggedIn: true,
      pageTitle: 'Add Transaction'
    }) %>

    <div class="max-w-2xl mx-auto mb-8">
      <div class="card p-4 md:p-6 lg:p-8 rounded-lg shadow-md">
        <form action="/addTransaction/<%= hisab._id %>" method="POST" class="space-y-6">
          <div class="form-group">
            <label for="description" class="form-label text-custom-primary">Description</label>
            <input
              type="text"
              id="description"
              name="description"
              required
              class="form-control bg-custom-white text-custom-primary border border-custom"
              placeholder="Enter transaction description"
            />
          </div>

          <div class="form-group">
            <label for="amount" class="form-label text-custom-primary">Amount</label>
            <input
              type="number"
              id="amount"
              name="amount"
              required
              min="0"
              step="0.01"
              class="form-control bg-custom-white text-custom-primary border border-custom"
              placeholder="0.00"
            />
          </div>

          <div class="form-group">
            <label class="form-label text-custom-primary">Transaction Type</label>
            <div class="flex gap-6 mt-2">
              <label class="inline-flex items-center bg-custom-gray p-3 rounded-lg cursor-pointer">
                <input
                  type="radio"
                  name="type"
                  value="credit"
                  required
                  class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-custom rounded"
                />
                <span class="ml-2 text-custom-primary">
                  <i class="fas fa-plus-circle text-green-600 mr-1"></i> Credit
                </span>
              </label>
              <label class="inline-flex items-center bg-custom-gray p-3 rounded-lg cursor-pointer">
                <input
                  type="radio"
                  name="type"
                  value="debit"
                  required
                  class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-custom rounded"
                />
                <span class="ml-2 text-custom-primary">
                  <i class="fas fa-minus-circle text-red-600 mr-1"></i> Debit
                </span>
              </label>
            </div>
          </div>

          <div class="flex flex-col sm:flex-row justify-end gap-3 mt-8">
            <a
              href="/viewHisab/<%= hisab._id %>"
              class="btn-secondary px-6 py-2 rounded-md text-center"
            >
              Cancel
            </a>
            <button
              type="submit"
              class="btn-primary px-6 py-2 rounded-md"
            >
              <i class="fas fa-plus-circle mr-2"></i> Add Transaction
            </button>
          </div>
        </form>
      </div>
    </div>

    <%- include('partials/footer', { isLoggedIn: true }) %>
  </body>
</html> 