<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Hisab - KhataBook</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50">
    <nav class="bg-white shadow-md">
      <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between items-center h-16">
          <div class="flex items-center">
            <a href="/dashboard" class="text-2xl font-bold text-blue-600">
              <i class="fas fa-book-open mr-2"></i>KhataBook
            </a>
          </div>
          <div class="flex items-center gap-3">
            <a href="/viewHisab/<%= hisab._id %>" class="px-4 py-2 rounded-md border border-blue-600 text-blue-600 hover:bg-blue-50">Back to Hisab</a>
            <a href="/logout" class="px-4 py-2 rounded-md bg-blue-600 text-white hover:bg-blue-700">LogOut</a>
          </div>
        </div>
      </div>
    </nav>

    <div class="max-w-2xl mx-auto mt-10 px-4">
      <div class="bg-white rounded-lg shadow-md p-8">
        <h1 class="text-3xl font-bold text-gray-900 mb-6">Edit Hisab</h1>
        
        <form action="/editHisab/<%= hisab._id %>" method="POST" class="space-y-6">
          <div>
            <label for="name" class="block text-sm font-medium text-gray-700">Hisab Name</label>
            <input
              type="text"
              id="name"
              name="name"
              value="<%= hisab.name %>"
              required
              class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500"
            />
          </div>

          <div>
            <label for="description" class="block text-sm font-medium text-gray-700">Description</label>
            <textarea
              id="description"
              name="description"
              required
              class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500"
            ><%= hisab.description %></textarea>
          </div>

          <div class="space-y-4">
            <div class="flex items-center">
              <input
                type="checkbox"
                id="isEncrypted"
                name="isEncrypted"
                <%= hisab.isEncrypted ? 'checked' : '' %>
                class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded"
              />
              <label for="isEncrypted" class="ml-2 block text-sm text-gray-900">
                Encrypt Hisab
              </label>
            </div>

            <div class="flex items-center">
              <input
                type="checkbox"
                id="isShareable"
                name="isShareable"
                <%= hisab.isShareable ? 'checked' : '' %>
                class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded"
              />
              <label for="isShareable" class="ml-2 block text-sm text-gray-900">
                Make Shareable
              </label>
            </div>
          </div>

          <div class="flex justify-end space-x-4">
            <a
              href="/viewHisab/<%= hisab._id %>"
              class="px-4 py-2 border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50"
            >
              Cancel
            </a>
            <button
              type="submit"
              class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700"
            >
              Save Changes
            </button>
          </div>
        </form>

        <div class="mt-8 pt-6 border-t border-gray-200">
          <form action="/deleteHisab/<%= hisab._id %>" method="POST" class="flex justify-end">
            <button
              type="submit"
              class="px-4 py-2 bg-red-600 text-white rounded-md hover:bg-red-700"
              onclick="return confirm('Are you sure you want to delete this hisab? This action cannot be undone.')"
            >
              Delete Hisab
            </button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html> 